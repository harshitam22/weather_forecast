{
  "name": "weather_forecast",
  "version": "1.0.0",
  "description": "creating a nodejs app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "harshita",
  "license": "ISC",
  "dependencies": {
    "requests": "^0.3.0"
  }
}
